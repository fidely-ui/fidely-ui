---
title: Heading
description: Used to render semantic HTML heading elements.
---

<ExampleTabs name="heading-basic" scope="heading" />

## Usage

```tsx
import { Heading } from '@snaps-ui/react'
```

```tsx
<Heading>Heading text.</Heading>
```

## Examples

### As another elements

By default, **Snaps UI** Heading renders as an h2 element.

Use the **as** prop to render it as another semantic element (e.g. h1, h3, or p).

<ExampleTabs name="heading-with-as" scope="heading" />

### Size

Use the **size** prop to adjust the visual size of the heading without changing its semantic tag. or pass **textStyle**

<ExampleTabs name="heading-size" scope="heading" />
