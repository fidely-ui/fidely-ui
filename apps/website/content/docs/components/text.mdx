---
title: Text
description: Used to render text and paragraphs.
---

<ExampleTabs name="text-basic" scope="text" />

## Usage

```tsx
import { Text } from '@snaps-ui/react'
```

```tsx
<Text>hello from snaps text component</Text>
```

## Examples

### Size

Use the **size** prop to adjust the visual size of the text or use the **textStyle** prop

<ExampleTabs name="text-size" scope="text" />

### Truncate

Use the **truncate** prop to truncate the text after a single line.

<ExampleTabs name="text-truncate" scope="text" />

### Line Clamp

Use the **lineClamp** prop to truncate the text after a certain number of lines.

<ExampleTabs name="text-line-clamp" scope="text" />

### Font weight

Use the **fontWeight** prop to change the weight of the text.

<ExampleTabs name="text-weight" scope="text" />
