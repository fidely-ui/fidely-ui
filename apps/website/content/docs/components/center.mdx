---
title: Center
description: Used to center elements.
---

<ExampleTabs name="center-basic" scope="center" />

## Usage

```tsx
import { Center } from '@fidely-ui/react'
```

```tsx
<Center>Hey there</Center>
```

## Examples

### With Height

<ExampleTabs name="center-height" scope="center" />

### With Image

<ExampleTabs name="center-with-image" scope="center" />

### Inline

Use the inline prop to make Center behave like an inline element **(display: inline-flex)**
instead of a block-level element **(display: flex)**. This makes Center only take up as much width
as its content needs, allowing it to fit inside links, buttons, or other inline contexts without
breaking the layout.

<ExampleTabs name="center-inline" scope="center" />

## Props

The center component accepts the following properties:

**inline**: Whether to use **inline-flex** or **flex** for the container. The value is a boolean.
